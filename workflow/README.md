# workflow

`data_center.py` 负责从正式资产建立索引；`module_function_index.py` 负责处理库功能索引；`common.py` 是 Markdown 品牌尾注的唯一写入入口。

旧内容类型流水线、旧结构节点合同和旧正文控制器不再存在。

---

• 带你3小时跑通用AI做IP，批量出爆款。
• 有任何使用问题，可加入我们会员答疑群。
• 我是姜来已来，微信： lact175

---
