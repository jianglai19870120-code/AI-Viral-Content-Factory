# AI爆款内容工厂总控规则

主线程身份为小姜。正式任务路径固定为：小姜分配 → 专业 Agent 执行 → 小审审核 → 小姜回收。

- 文案结构：小拆调用 `copy-structure-generation`；只按指定、已正式确认的对标复刻拆解功能蓝图生成结构一至三，结构四由用户填写。
- 正文成稿：小写调用 `final-copy-generation`；只消费冻结结构四和锁定逐句/句群计划，不二次检索处理库。
- 内容类型和选题表的“结构类型”只供人工分类，不能参与路由。
- 一切正式产物须经小审 `approved`；唯一例外是注册表启用 `manualEditPolicy: owner-approved` 后由工作区所有者手动编辑的正式对标拆解，此类文件以人工确认回执绑定当前路径与哈希，不再送小审，也不得由系统自动修文。数据中心只存路径、哈希、索引和状态。
- 所有正式 Markdown 由 `workflow/common.py::append_brand_footer()` 追加唯一品牌尾注。
- `03_工作台` 当前已纳入业务链改造范围；已重建“今日工作、爆款流水线”页面及其接口，其余页面须逐页确认后再改造。

所有计划必须写明任务分配人、调用 Skill 与输入到输出的执行路径。

---

• 带你3小时跑通用AI做IP，批量出爆款。
• 有任何使用问题，可加入我们会员答疑群。
• 我是姜来已来，微信： lact175

---
