调用 `$breakdown-work-journal`（今日复盘-案例卡拆解Skill（会员专享）），执行“同步 IMA 今日复盘 + 处理当前待拆解资料 + 生成案例卡”的完整正式链路。

先完整读取当前项目系统说明、中央注册表、`10_Skills武器库/01_小姜调度Skill/SKILL.md` 与 `10_Skills武器库/今日复盘-案例卡拆解Skill（会员专享）/SKILL.md`，严格按正式调度、审核、输出规则执行。

执行要求：

1. 必须先按小姜口径创建或确认正式调度记录，再进入小拆执行；不得绕过调度门禁直接跑脚本。
2. IMA 同步默认先走官方 `ima-skill` 主路径；若用户给的是 legacy `notebook_id`，必须先自动解析/映射到当前可用 `knowledge_base_id`，不得因 ID 形态变化直接报错。
3. 同步时必须先检查官方 `ima-skill`、`IMA_OPENAPI_CLIENTID`、`IMA_OPENAPI_APIKEY`、知识库连通性与新增条目状态；如果官方路径只能拿到音频地址、拿不到可直接落盘的转写文本，才进入本地转写补齐链路。
4. “同步无新增”不等于“本次任务结束”。无论同步结果是 `已更新` 还是 `无更新`，都必须继续扫描当前输入库里所有待拆解今日复盘资料，区分：
   - 可直接晋升
   - 缺候选 JSON
   - 测试/噪音拦截
   - 缺一级证据拦截
5. 如果当前没有待拆解资料，必须明确回报“本次同步新增 0 条、待拆解 0 条、未生成新案例卡”，不要笼统写“已完成”。
6. 如果存在待拆解资料，必须继续按 Skill 规则执行入口闸门、候选生成、正式受理与小审审核；未通过小审的候选不得交付，不得写成正式完成。
7. 若脚本在 Windows 控制台输出阶段出现编码问题，只能修正输出方式或改用 UTF-8 重跑，不得把编码报错误判成业务失败。
8. 最终汇报必须分别说明：
   - IMA 同步结果
   - 待拆解扫描结果
   - 本次新生成/新放行的案例卡数量
   - 若为 0 项，也要显式写 0
